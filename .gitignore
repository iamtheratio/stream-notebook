node_modules/

# Everything user-specific lives here: notes database, OAuth tokens, settings.
# Never commit it — settings.json holds a Twitch access token.
data/

# Editor-local; the audience for this repo does not use VS Code.
.vscode/

npm-debug.log*
.DS_Store
Thumbs.db
